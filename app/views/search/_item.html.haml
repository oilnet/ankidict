%tr
  %td.source_lang{:lang => :de}= link_to(item.de.html_safe, :action => :details, :id => item.id)
  %td.target_lang{:lang => :ar}= link_to(item.ar_singular_or_perfect.html_safe, :action => :details, :id => item.id)
